# .env.example
# ----------------------------------------------------------------------------------
# This file lists all environment variables required to run the Polyglot Goodreads 
# application. You should copy this file to .env and fill in your actual credentials.
# ----------------------------------------------------------------------------------

# --- 🔑 JWT & Security ---
# CRITICAL: This secret key is used to sign and verify JWT tokens.
# MUST match the key used across all security files (e.g., utils/security.py).
JWT_SECRET_KEY="A_SECURE_RANDOM_STRING_FOR_JWT_SIGNING"


# --- 🐘 PostgreSQL (Transactional Data) ---
# NOTE: If running via Docker Compose, set POSTGRES_HOST=postgres
POSTGRES_USER=root
POSTGRES_PASSWORD=root
POSTGRES_DB=goodreads
POSTGRES_HOST=postgres
POSTGRES_PORT=5432


# --- 🍃 MongoDB (Document/Catalog Data) ---
# NOTE: If running via Docker Compose, set MONGO_HOST=mongo
MONGO_HOST=mongo
MONGO_PORT=27017
MONGO_DB=goodreads
# You may need to update the MONGO_URI if using a different setup
# MONGO_URI=mongodb://${MONGO_HOST}:${MONGO_PORT}/${MONGO_DB}


# --- ⚡ Redis (Session Cache) ---
# NOTE: If running via Docker Compose, set REDIS_HOST=redis
REDIS_HOST=redis
REDIS_PORT=6379


# --- 🌳 Neo4j (Graph/Social Data) ---
# NOTE: If running via Docker Compose, set NEO4J_HOST=neo4j
NEO4J_URI=neo4j://neo4j:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=password123